<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Admin Panel - Nova Lojistik</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="admin-body">
<nav class="navbar navbar-dark border-bottom border-secondary-subtle admin-navbar">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 h6">Nova Lojistik - İçerik Yönetimi</span>
    <div class="d-flex align-items-center gap-2">
      <a href="/" class="btn btn-sm btn-outline-light" target="_blank">Siteyi Görüntüle</a>
      <a href="/admin/logout" class="btn btn-sm btn-outline-danger">Çıkış Yap</a>
    </div>
  </div>
</nav>

<div class="container my-4">
  <% if (message) { %>
    <div class="alert alert-success py-2"><%= message %></div>
  <% } %>

  <form method="post" action="/admin">
    <div class="row g-4">
      <div class="col-lg-6">
        <div class="card admin-card p-3 mb-3">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <h6 class="mb-0">Hero Alanı</h6>
            <span class="badge badge-section px-2 py-1">Sayfanın en üstündeki büyük başlık</span>
          </div>
          <div class="mb-2">
            <label>Başlık</label>
            <input type="text" name="hero_title" class="form-control" value="<%= content.hero_title %>">
          </div>
          <div class="mb-2">
            <label>Kısa Açıklama</label>
            <textarea name="hero_subtitle" class="form-control" rows="3"><%= content.hero_subtitle %></textarea>
          </div>
          <div class="mb-2">
            <label>Buton Yazısı</label>
            <input type="text" name="hero_cta" class="form-control" value="<%= content.hero_cta %>">
          </div>
        </div>

        <div class="card admin-card p-3 mb-3">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <h6 class="mb-0">Hakkımızda Alanı</h6>
            <span class="badge badge-section px-2 py-1">Firmanın kısa tanımı</span>
          </div>
          <div class="mb-2">
            <label>Başlık</label>
            <input type="text" name="about_title" class="form-control" value="<%= content.about_title %>">
          </div>
          <div class="mb-2">
            <label>Metin</label>
            <textarea name="about_text" class="form-control" rows="5"><%= content.about_text %></textarea>
          </div>
        </div>

        <div class="card admin-card p-3 mb-3">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <h6 class="mb-0">İletişim Bilgileri</h6>
            <span class="badge badge-section px-2 py-1">Alt kısımda görünen iletişim alanı</span>
          </div>
          <div class="mb-2">
            <label>İletişim Başlığı</label>
            <input type="text" name="contact_title" class="form-control" value="<%= content.contact_title %>">
          </div>
          <div class="mb-2">
            <label>İletişim Metni</label>
            <textarea name="contact_text" class="form-control" rows="4"><%= content.contact_text %></textarea>
          </div>
          <div class="mb-2">
            <label>Telefon</label>
            <input type="text" name="company_phone" class="form-control" value="<%= content.company_phone %>">
          </div>
          <div class="mb-2">
            <label>E-posta</label>
            <input type="email" name="company_email" class="form-control" value="<%= content.company_email %>">
          </div>
          <div class="mb-2">
            <label>Adres</label>
            <textarea name="company_address" class="form-control" rows="3"><%= content.company_address %></textarea>
          </div>
        </div>
      </div>

      <div class="col-lg-6">
        <div class="card admin-card p-3 mb-3">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <h6 class="mb-0">Hizmetler Başlığı</h6>
            <span class="badge badge-section px-2 py-1">Hizmetler bölümünün ana başlığı</span>
          </div>
          <div class="mb-2">
            <label>Başlık</label>
            <input type="text" name="services_title" class="form-control" value="<%= content.services_title %>">
          </div>
        </div>

        <div class="card admin-card p-3 mb-3">
          <h6 class="mb-2">Hizmet 1 (Karayolu)</h6>
          <div class="mb-2">
            <label>Başlık</label>
            <input type="text" name="service1_title" class="form-control" value="<%= content.service1_title %>">
          </div>
          <div class="mb-2">
            <label>Metin</label>
            <textarea name="service1_text" class="form-control" rows="3"><%= content.service1_text %></textarea>
          </div>
        </div>

        <div class="card admin-card p-3 mb-3">
          <h6 class="mb-2">Hizmet 2 (Uluslararası)</h6>
          <div class="mb-2">
            <label>Başlık</label>
            <input type="text" name="service2_title" class="form-control" value="<%= content.service2_title %>">
          </div>
          <div class="mb-2">
            <label>Metin</label>
            <textarea name="service2_text" class="form-control" rows="3"><%= content.service2_text %></textarea>
          </div>
        </div>

        <div class="card admin-card p-3 mb-3">
          <h6 class="mb-2">Hizmet 3 (Depolama & Dağıtım)</h6>
          <div class="mb-2">
            <label>Başlık</label>
            <input type="text" name="service3_title" class="form-control" value="<%= content.service3_title %>">
          </div>
          <div class="mb-2">
            <label>Metin</label>
            <textarea name="service3_text" class="form-control" rows="3"><%= content.service3_text %></textarea>
          </div>
        </div>
      </div>
    </div>

    <div class="text-end mt-3">
      <button type="submit" class="btn btn-primary-soft px-4">Değişiklikleri Kaydet</button>
    </div>
  </form>
</div>
</body>
</html>
