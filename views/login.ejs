<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Admin Giriş - Nova Lojistik</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="admin-body">
<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-5">
      <div class="card glass-card p-4 shadow-lg mt-5">
        <div class="d-flex justify-content-between align-items-center mb-3">
          <h5 class="mb-0">Nova Lojistik Admin</h5>
          <span class="logo-pill">İçerik Yönetim Paneli</span>
        </div>
        <p class="mb-3 text-muted small">Tek sayfalık sitenin başlık ve metinlerini buradan düzenleyebilirsiniz.</p>
        <% if (error) { %>
          <div class="alert alert-danger py-2"><%= error %></div>
        <% } %>
        <form method="post" action="/admin/login">
          <div class="mb-3">
            <label class="form-label">Kullanıcı Adı</label>
            <input type="text" name="username" class="form-control" required value="admin">
          </div>
          <div class="mb-3">
            <label class="form-label">Şifre</label>
            <input type="password" name="password" class="form-control" required value="123456">
          </div>
          <button type="submit" class="btn btn-primary-soft w-100">Giriş Yap</button>
        </form>
        <p class="mb-0 mt-3 text-muted tiny">Bu panel sadece metin ve başlık düzenlemek için tasarlanmıştır. Tasarım değişiklikleri için geliştiricinizle iletişime geçebilirsiniz.</p>
      </div>
    </div>
  </div>
</div>
</body>
</html>
